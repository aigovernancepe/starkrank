---
import { getCollection } from 'astro:content';
import BaseLayout from '../layouts/BaseLayout.astro';
import HeroSection from '../components/ui/HeroSection.astro';
import Section from '../components/ui/Section.astro';
import SectionHeader from '../components/ui/SectionHeader.astro';
import PillarCard from '../components/ui/PillarCard.astro';
import Button from '../components/ui/Button.astro';
import CTABanner from '../components/ui/CTABanner.astro';
import { pillars } from '../data/pillars';
import { getSpokePath } from '../data/service-slugs';

const pillarCityLinks: Record<string, string> = {
  'search-ai-intelligence': `/${getSpokePath('ai-search-optimization', 'fort-lauderdale', 'en')}/`,
  'performance-media': `/${getSpokePath('google-ads-management', 'fort-lauderdale', 'en')}/`,
  'authority-pr': `/${getSpokePath('reactive-pr', 'fort-lauderdale', 'en')}/`,
  'content-copywriting': `/${getSpokePath('seo-copywriting', 'fort-lauderdale', 'en')}/`,
  'data-web': `/${getSpokePath('google-analytics-consultancy', 'fort-lauderdale', 'en')}/`,
};

const allSpokes = await getCollection('spokes', (s) => s.data.locale === 'en' && s.data.citySlug === 'fort-lauderdale');
const spokesByPillar = pillars.map((pillar) => ({
  pillar,
  spokes: pillar.services
    .flatMap((serviceSlug) => allSpokes.filter((s) => s.data.serviceSlug === serviceSlug))
    .map((s) => ({
      title: s.data.title,
      description: s.data.description,
      href: `/${getSpokePath(s.data.serviceSlug, s.data.citySlug, 'en')}/`,
    })),
}));

const placeSchema = {
  '@context': 'https://schema.org',
  '@type': 'Place',
  name: 'Fort Lauderdale, Florida',
  address: {
    '@type': 'PostalAddress',
    addressLocality: 'Fort Lauderdale',
    addressRegion: 'FL',
    addressCountry: 'US',
  },
  geo: {
    '@type': 'GeoCoordinates',
    latitude: 26.1224,
    longitude: -80.1373,
  },
};
---

<BaseLayout
  title="Digital Marketing Fort Lauderdale — StarkRank Florida"
  description="StarkRank helps Greater Fort Lauderdale businesses compete in traditional and AI-powered search with data-driven strategies — local SEO, technical SEO, performance media, and content that converts."
  schema={[placeSchema]}
>
  <HeroSection
    headline="Fort Lauderdale Digital Marketing — **Growth, Locally Earned**"
    subheadline="Greater Fort Lauderdale is one of the fastest-growing business corridors in the US. StarkRank helps local companies win visibility in traditional and AI-powered search through data-driven local SEO, technical SEO, and performance media."
  >
    <Button href="/free-google-ads-audit/" variant="primary" size="lg">Free Google Ads Audit</Button>
    <Button href="/contact/" variant="secondary" size="lg">Get in Touch</Button>
  </HeroSection>

  <Section bg="cream">
    <SectionHeader
      label="What We Do in Fort Lauderdale"
      title="Five Pillars of Digital Growth"
      subtitle="From AI search intelligence to transparent ROI reporting — the full stack of digital marketing, delivered locally in Fort Lauderdale."
      align="center"
    />
    <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-6">
      {pillars.map((pillar) => <PillarCard pillar={pillar} href={pillarCityLinks[pillar.id]} />)}
    </div>
  </Section>

  <Section bg="white">
    <SectionHeader
      label="Services in Fort Lauderdale"
      title="The Full Service Catalogue"
      subtitle="Every service we offer, available to Fort Lauderdale businesses. Pick the engine you need, or combine them into a full-funnel strategy."
      align="center"
    />
    <div class="space-y-12">
      {spokesByPillar.map(({ pillar, spokes }) => spokes.length > 0 && (
        <div>
          <h3 class="text-sm font-label font-bold uppercase tracking-[0.2em] text-blue mb-4">{pillar.name}</h3>
          <div class="grid grid-cols-1 md:grid-cols-2 lg:grid-cols-3 gap-4">
            {spokes.map((spoke) => (
              <a
                href={spoke.href}
                class="group block bg-cream/40 border border-gray-light/40 rounded-lg p-5 transition-all hover:shadow-md hover:border-blue/30"
              >
                <h4 class="text-base font-headline font-black text-navy group-hover:text-blue transition-colors leading-snug">
                  {spoke.title}
                </h4>
                <p class="mt-2 text-sm font-body text-gray leading-relaxed line-clamp-2">
                  {spoke.description}
                </p>
              </a>
            ))}
          </div>
        </div>
      ))}
    </div>
  </Section>

  <CTABanner
    bg="navy"
    title="Is Your Ad Spend Working in Fort Lauderdale?"
    subtitle="Get a free Google Ads audit and discover where your budget is leaking."
  >
    <Button href="/free-google-ads-audit/" variant="primary" size="lg">Free Ads Audit</Button>
    <Button href="/free-copywriting-audit/" variant="ghost" size="lg">Free Copywriting Audit</Button>
  </CTABanner>
</BaseLayout>
