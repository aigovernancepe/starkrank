---
import BaseLayout from '../../layouts/BaseLayout.astro';
import ServiceFinderApp from '../../components/ServiceFinder/ServiceFinderApp';
import { es } from '../../data/service-finder/locales/es';

const webAppSchema = {
  '@context': 'https://schema.org',
  '@type': 'WebApplication',
  name: 'Diagnóstico Rápido',
  alternateName: ['Find Your Service', 'Service Schnellcheck'],
  description: 'Descubre qué servicios de marketing digital necesita tu negocio — en 60 segundos.',
  url: 'https://starkrank.com/pe/diagnostico-rapido/',
  applicationCategory: 'BusinessApplication',
  operatingSystem: 'Web',
  offers: { '@type': 'Offer', price: '0', priceCurrency: 'PEN' },
  provider: { '@type': 'Organization', name: 'StarkRank LLC', url: 'https://starkrank.com' },
  inLanguage: ['es', 'en', 'de'],
};

const faqSchema = {
  '@context': 'https://schema.org',
  '@type': 'FAQPage',
  mainEntity: es.faq.map(f => ({
    '@type': 'Question',
    name: f.question,
    acceptedAnswer: { '@type': 'Answer', text: f.answer },
  })),
};

const hreflangAlternates = [
  { hreflang: 'en', href: 'https://starkrank.com/find-your-service/' },
  { hreflang: 'es-PE', href: 'https://starkrank.com/pe/diagnostico-rapido/' },
  { hreflang: 'de-DE', href: 'https://starkrank.com/de/schnellcheck/' },
  { hreflang: 'de-CH', href: 'https://starkrank.com/ch-de/schnellcheck/' },
  { hreflang: 'x-default', href: 'https://starkrank.com/find-your-service/' },
];
---

<BaseLayout
  title="Diagnóstico Rápido — Descubre qué Servicios Necesita tu Negocio | StarkRank"
  description="Responde 4 preguntas y descubre qué servicios de marketing digital necesita tu negocio. Gratis, sin registro."
  locale="pe"
  schema={[webAppSchema, faqSchema]}
>
  <Fragment slot="head">
    {hreflangAlternates.map(({ hreflang, href }) => (
      <link rel="alternate" hreflang={hreflang} href={href} />
    ))}
  </Fragment>

  <section class="max-w-3xl mx-auto px-4 pt-16 pb-8 text-center">
    <h1 class="text-3xl md:text-4xl mb-4">{es.intro.h1}</h1>
    <p class="text-lg text-gray max-w-2xl mx-auto mb-3">{es.intro.subheading}</p>
    <p class="text-sm text-gray">{es.intro.trustLine}</p>
  </section>

  <ServiceFinderApp client:visible locale={es} market="es" />

  <section class="max-w-3xl mx-auto px-4 py-16">
    <h2 class="text-2xl mb-8 text-center">Preguntas Frecuentes</h2>
    {es.faq.map(item => (
      <details class="mb-4 bg-white rounded-lg border border-gray-light/40">
        <summary class="px-6 py-4 cursor-pointer font-headline font-bold text-navy text-[0.9375rem] list-none flex items-center justify-between">
          {item.question}
          <svg class="w-5 h-5 text-gray flex-shrink-0 ml-4 transition-transform" viewBox="0 0 20 20" fill="currentColor"><path fill-rule="evenodd" d="M5.293 7.293a1 1 0 011.414 0L10 10.586l3.293-3.293a1 1 0 111.414 1.414l-4 4a1 1 0 01-1.414 0l-4-4a1 1 0 010-1.414z" clip-rule="evenodd" /></svg>
        </summary>
        <div class="px-6 pb-4 text-gray text-[0.875rem] leading-relaxed" set:html={item.answer} />
      </details>
    ))}
  </section>
</BaseLayout>
